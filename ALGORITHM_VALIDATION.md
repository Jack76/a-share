# 算法重放与验收基线

## 历史重放输入

`runDecisionReplay` 只接受按时间切片的 `ReplaySnapshot[]`。每个快照必须包含：

- 当时可见的个股数据与历史 K 线，严禁携带未来数据；
- 不少于 4,000 只股票的同时刻全市场截面；
- 市场宽度、阶段置信度、指数与数据质量状态；
- 同时刻的指数快照和 `DailyMetrics`，用于重放黑天鹅/狂热组合熔断；
- 当时的题材/板块排名与事件驱动上下文。

严格模式默认开启：未包含目标股、全市场覆盖率低于 85%、市场数据过期或历史 K 线越界时直接拒绝重放。

## 成交约束

- 信号在当前快照生成，买入最早于下一快照成交；
- 按 A 股 100 股整手下单，买入当日不允许卖出；
- 涨停买单和跌停卖单不假设必然成交；
- 同日止损与目标同时触发时按止损优先；
- 佣金、卖出税费、滑点均由 `ReplayConfig` 显式配置。

## 默认验收门槛

- 已平仓样本不少于 30 笔；
- 样本外单笔期望值大于 0；
- Profit Factor 不低于 1.2；
- 最大回撤不高于 15%；
- Brier Score 不高于 0.25；
- 预期校准误差 ECE 不高于 0.10。

验收结果同时按 `RISK_ON / RISK_OFF / DIVERGENT / NEUTRAL / UNKNOWN` 分层输出，禁止仅用全样本平均值掩盖特定市场阶段失效。
