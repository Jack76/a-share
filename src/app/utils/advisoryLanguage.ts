export const sanitizeAdvisoryLanguage = (text: string | undefined) => (text || '')
  .replaceAll('【内幕】', '【依据】')
  .replaceAll('内幕', '依据')
  .replaceAll('AI模型', '规则模型')
  .replaceAll('AI 模型', '规则模型')
  .replaceAll('AI锁仓信号', '规则持有信号')
  .replaceAll('AI已', '规则引擎已')
  .replaceAll('AI', '规则')
  .replaceAll('主力成本线', '量价估算成本线')
  .replaceAll('主力成本', '量价估算成本')
  .replaceAll('主力浮盈', '现价相对量价估算成本偏离')
  .replaceAll('主力被套', '现价低于量价估算成本')
  .replaceAll('主力自救', '价格修复')
  .replaceAll('主力资金', '大单与量价')
  .replaceAll('主力控盘', '低换手锁仓特征')
  .replaceAll('主力', '资金行为')
  .replaceAll('暗盘', '量价')
  .replaceAll('黄金坑', '深度回调')
  .replaceAll('胜率极高', '仍需验证')
  .replaceAll('高胜率', '需验证')
  .replaceAll('千载难逢', '规则筛选出的')
  .replaceAll('重仓出击', '仅在风险预算内分批执行')
  .replaceAll('死死拿住', '持有并执行退出条件')
  .replaceAll('坚决锁仓', '持有并执行退出条件')
  .replaceAll('无视所有超买指标', '不可仅依据超买指标退出')
  .replaceAll('忽略一切超买指标', '不可仅依据超买指标退出')
  .replaceAll('无视盘中', '持续核对盘中')
  .replaceAll('置信度高达', '规则信心为')
  .replaceAll('确定性买点', '待验证买点')
  .replaceAll('最确定的', '规则一致性较高的')
  .replaceAll('极大可能', '存在')
  .replaceAll('大概率', '规则推测')
  .replaceAll('疯狂吸筹', '出现承接特征')
  .replaceAll('坚决上车', '等待确认并控制仓位')
  .replaceAll('立即无条件市价止损', '按预设止损规则执行')
  .replaceAll('立即清仓', '优先执行退出计划');
